name: Test Workflow

on: [push]

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Print message
        run: echo "Hello from GitHub Actions!"